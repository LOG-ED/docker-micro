syntax = "proto3";

service Task {
	rpc Run(RunRequest) returns (RunResponse) {}
}

message RunRequest {
	string command = 1;
}

message RunResponse {
	enum Status {
		UNKNOWN = 0;
		COMPLETED = 200;
		FAILED = 500;
	}

	Status status = 1;
}